# Week 3 数据库复现说明

> 状态：待实现。当前 `db/` 只有本说明；`schema.sql`、`sample-data.sql`、`crud.sql` 和实际执行结果尚未提交，因此尚未完成从空库复现。

## 目标环境

- 数据库：SQL Server，使用 SSMS 执行 T-SQL。
- 实际验证时补记 SQL Server 版本、实例名、数据库名、执行者与日期；连接凭据不要写进仓库。
- 先确认目标是专用的空数据库，不要对现有经营数据执行重建脚本。

## 计划执行顺序

1. 运行 `schema.sql`：创建数据库、19 张表及 Week 3 要求的基础约束。
2. 运行 `sample-data.sql`：按依赖顺序插入 `docs/spec-data-dictionary.md` 的样例元组；先有店长员工，再装入银卡、金卡两条当前配置，随后插入会员与业务单据。
3. 执行结构与数据校验：检查表、字段、主外键、样例行数以及库存、积分、资金对账。
4. 运行 `crud.sql`：在事务中演示商品、库存相关记录和订单的 CRUD，记录前后结果，并回滚演示事务。
5. 在重新建立的空库上再次执行第 1—4 步，确认结果一致。

Week 4 的 `query.sql`、`view.sql`、`constraint.sql`、`role.sql` 在 Week 3 脚本可复现后再按该周任务补充；`constraint.sql` 不重复创建 `schema.sql` 已建的约束。

## 实际复现记录

脚本完成后，在本节写明每一步的具体执行方式、预期和实际结果、校验 SQL；CRUD 输出与截图索引放在 `results/week-3.md`。由非脚本作者组员独立执行并记录失败及修正。本节目前没有执行结果，不能视为验收通过。
