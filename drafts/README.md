# 历史过程稿

本目录保留 `plan-week-1.md` 和 `plan-week-2.md`，用于追溯早期方案及人工裁定。它们记录的是当时的编号、表数和设计，可能与当前正式文档不同；实现、测试和验收以 `docs/spec-*.md` 与当周 `docs/task-week-*.md` 为准。

旧的总览、情景、测试纲要、业务对照、边界清单和 MySQL 表设计稿已与正式文档重复或冲突。可用的「退货单 + 退货明细」结构及退款关联已纳入正式数据字典，其余旧稿不再保留。
