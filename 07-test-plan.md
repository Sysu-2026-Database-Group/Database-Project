# 07 · 测试纲要（语义层）

> 本文档**只说明「测什么、要达到什么语义标准」**，不写具体测试数据、操作步骤与验证 SQL。
> 细节测试方案留待后续讨论（见 §6）。
>
> **v8 修订**（2026-09-11）：按「第一阶段只做初步拍案、不敲细节」的方针**整体瘦身** ——
> 原先的数据基线表、逐用例步骤表、预期数值表、边界用例编号表与全部验证 SQL 一律移除，
> 只保留**测试内容的语义**。
>
> **v7 修订**（2026-09-11）：建档状态改判为 `OUT_OF_STOCK`（R13）；
> 线上预订不冻结金额 / 折扣 / 积分（R11）；盈亏拆为两个口径（R12）；
> 撤销一条与另一条语义重复的校验项（该编号体系已随 v8 一并移除）。
>
> **v6 修订**（2026-09-11）：「已预定」不再影响商品状态；
> 商品状态收敛为 4 态，判定退回一元 `fn_ProductStatus(@stock_qty)`。

---

## 1. 测试原则

1. **不测界面，测数据**。验收标准是**表里的数据对不对**，不是页面好不好看。
2. **每条用例跑在干净的数据基线上**，用例之间互不污染。
3. **每条用例结束前必须校验全局自洽性**（见 §5）。
4. **事务可回滚**：单条用例应能包裹在一个事务里跑完再 `ROLLBACK`，便于反复验证而不留痕。

---

## 2. 测试范围

| 层次 | 覆盖什么 |
|---|---|
| 业务层 | **7 个规则域**（`01` §2.1 ~ §2.7）的端到端语义 |
| 角色层 | **5 个角色**的职责边界（正例 + 反例） |
| 数据层 | **全局自洽性**（见 §5） |

---

## 3. 业务场景测试（语义）

本表按**可验证的场景**组织，**不强制与 `01` §2 的规则域一一对应** ——
有些场景横跨多个规则域（例如退货同时涉及 §2.3 的计价与 §2.5 的积分回滚）。
每条只说明**要验证的语义**，不写步骤。

| 编号 | 场景 | 对应 `01` | 要验证的语义 |
|---|---|---|---|
| **TC-B1** | 创店初始化 | §2.1 / §2.7 | 角色与权限矩阵建立完毕；图书与文创建档后**子类型完整**、**初始状态为缺货**（可售余量为 0，不因建档而变成在售）；店长注资**只增加现金余额，不计入经营盈亏** |
| **TC-B2** | 采购 → 收货 | §2.2 | 采购单走完「下单 → 收货」两个动作；**入库产生库存流水与采购支出**，两者在同一事务内；商品因可售余量由 0 变正而**自动由缺货转在售** |
| **TC-B3** | 线下销售 | §2.3 / §2.5 | 计价严格遵循「**先扣积分，再打折**」；成交价、折扣被**快照**进订单行；库存、积分、累计消费、收入**四者同步变化**，缺一不可 |
| **TC-B4** | 退货（含部分退货） | §2.3 / §2.5 | **按订单行**退货可用；退款按**订单行快照**计算而非当前售价；积分与累计消费同步回滚；**会员等级不因退货下调** |
| **TC-B5** | 账面统计 | §2.7 | **经营盈亏与现金余额是两个不同的数**；注资 / 提现只进现金余额，不进经营盈亏 |
| **TC-B6** | 线上预订 → 到店自提 | §2.6 | 预订**只锁住商品数量，不冻结金额、折扣与积分**；库存变化发生在**下单预占**时刻；**自提时才重新计价并生成销售订单**；只有自提才产生收入 |
| **TC-B7** | 会员系统 | §2.5 | 办卡 → 查积分 → 消费时享折扣与积分抵扣 → 升级的完整链路 |

---

## 4. 角色权限测试（语义）

每个角色用一个**正例**（应成功）与一个**反例**（应被拒绝）的思路来验证边界。

| 角色 | 正例语义（可做） | 反例语义（不可做） |
|---|---|---|
| **管理员** `ADMIN` | 用户与角色管理、基础数据维护、线上预订系统与会员系统的参数维护、全部查询 | 直接修改派生字段（库存、积分、状态） |
| **店长** `MANAGER` | 查看经营盈亏与现金余额、注资与提现、登记日常费用、会员管理、发起盘点 | 直接修改派生字段；提现超过账面现金 |
| **收银员** `CASHIER` | 门店开单、开通会员、线上预订核销、取消预订、受理退货 | 修改商品售价；注资 / 提现 |
| **库管** `STOCKER` | 商品建档、采购收货、报损、执行盘点、退货回收 | 查看财务报表；核销线上预订 |
| **顾客** `MEMBER` | 浏览商品、线上预订、取消自己的预订、开通会员、查询自己的积分与历史 | 访问后台任何维护页面；查看他人的订单、预订或积分 |

---

## 5. 数据自洽性（语义）

所有业务动作执行完毕后，系统必须**同时**满足以下几类自洽性。
这里只列**语义**，不写校验 SQL（验证方式留待后续讨论）。

| 维度 | 必须自洽的内容 |
|---|---|
| 库存 | 可售量 = 全部库存流水的求和 |
| 库存 | 可售量、已订量都不为负 |
| 库存 | **可售量 + 已订量 = 架上实物总量**，也是**盘点的账面基准** |
| 商品 | **自动区状态永远等于按可售余量判定的结果**（一元判定，不得漂移） |
| 商品 | **已订量无论怎么变化，都不影响商品状态** |
| 商品 | **人工区状态（下架 / 停售）在库存变动时不被触发器改动** |
| 预订 | 已订量 = 有效预订单的行数量之和 |
| 预订 | **自提成功 ⟺ 预订单挂有销售订单**，且一张预订单最多生成一张订单 |
| 预订 | 预订单的预占与释放都有对应的库存流水 |
| 订单 | 订单实付 = （小计 − 积分抵扣）× 折扣率 |
| 订单 | 累计退货量不超过购买量；每张已收款订单有且仅有一条收入流水 |
| 会员 | 积分余额 = 积分流水求和 |
| 资金 | 现金余额不为负；每张已收货的采购单必有对应支出流水 |
| 数据 | 子类型完整：图书必有图书扩展、文创必有文创扩展，且不交叉 |

### 5.1 必须守住的几条设计承诺（关键边界语义）

以下几句话是本设计的**核心承诺**，任何一条被破坏都说明模型有漏洞：

1. **一册预订不得锁死整个种类**：20 册里只被订走 1 册时，商品必须**仍然在售**，其余 19 册照常可卖。
   「全部被订走」与「全部卖光」在**可执行的动作上完全一致**，因此共用同一个状态（缺货）。
2. **已预占的商品不可报损**：报损只从可售量扣减，可售余量为 0 时校验自然失败。
3. **已超时的预订不得被核销**；同一取货码不得核销两次。
4. **并发抢同一库存时只能有一笔成功**。
5. **派生字段被篡改时应被对账查出**（库存、已订量、商品状态、积分余额四类）。
6. **人工区状态不被库存变动干扰**：商品处于下架 / 停售期间，进货或卖出都不得改动其状态。

---

## 6. 验收标准

- [ ] 7 个业务场景（TC-B1 ~ TC-B7）的**语义**全部成立
- [ ] 5 个角色的**正例与反例**全部符合预期
- [ ] §5 的每一类自洽性都成立，§5.1 的每一条设计承诺都守住
- [ ] 预订单状态机与商品状态机的**全部迁移路径**都被覆盖
- [ ] 每个业务流都能在**单个事务**内完成并成功 `ROLLBACK`

---

## 7. 留待讨论（本阶段不做）

以下属于**细节测试**，按「第一阶段只做初步拍案」的方针暂不展开：

| 编号 | 留待讨论的内容 | 备注 |
|---|---|---|
| T1 | **具体测试数据基线的数值**（表数、条数、金额、会员分布） | 需与 `04` 数据字典同步定 |
| T2 | **逐条用例的操作步骤与预期数据变化表** | 依赖 T1 |
| T3 | **边界与异常用例的编号清单**（并发、重复核销、篡改、超时等） | §5.1 已给出语义，编号与具体构造待定 |
| T4 | **每条自洽性的验证 SQL 与校验视图** | 依赖 `05` 建库脚本 |
| T5 | **测试执行顺序、回归方案与数据重建脚本** | 依赖 T1 |
