# 00 · 项目总览

> **一句话定位**  
> 一个以「单门店 + 线上预订自提」为背景的书店运营系统。  
> 目标不是做出一个网站，而是把「钱、书、人」在时间轴上的流动，  
> 落成 SQL Server 上一套可审查、可对账、可回滚的表、约束与事务。



---

## 1. 课程约束

| 项     | 内容                        |
| ----- | ------------------------- |
| 课程    | 数据库（本科课程设计）               |
| 团队    | 3 人小组                     |
| 周期    | 3 个月，分 3 个阶段              |
| 后端数据库 | SQL Server Express + SSMS |
| 交付形态  | B/S 架构 + 简洁前端 UI          |
| 目标难度  | 适中                        |

---

## 2. 项目重心声明（最重要的一节）

本项目**不是**一个 Web 工程练习，而是一次数据库设计练习。  
判断做得好不好，只看两条：

1. **数据建模是否规范** —— ER 是否正确、关系模式是否达到 3NF、约束是否完整。
2. **数据流动是否可解释** —— 每个业务动作引起的表级变化，能否说清、查到、回滚。

> **说明（v8）**：承载上述两条的两份文档 —— 数据流动地图与 ER 模型 ——
> 已按「第一阶段只做初步拍案」的方针**暂时移除**，留待后续讨论。
> 本阶段先冻结**规则**（`01`）与**验收语义**（`07`）；
> 建模层面的具体产物，等进入数据字典阶段再重新展开。

由此推出一条关键结论：**应用层要刻意做薄**。

- 不做分层架构，不做 Repository / Service 抽象，不引入重型 ORM。
- 每一条 SQL 都必须能被人在代码里逐行读出来。
- 业务规则尽量下沉到数据库（约束、触发器、存储过程），而不是写在应用代码里。

一句话：**数据库是主角，应用只是它的遥控器。**

---

## 3. 六个关键决策（已定案）

| 决策         | 定案                                                                          | 附带的设计考点                           |
| ---------- | --------------------------------------------------------------------------- | --------------------------------- |
| 书店形态       | 单门店 + 线上预订自提（无物流）                                                           | 订单状态机、线上线下共享库存的并发                 |
| 商品范围       | 新书 + 文创（无规格变体），不做二手书                                                        | 商品泛化：`Product` ← `Book` / `Goods` |
| 库存真源       | 流水账为真源 + **两个计数**（可售 / 已预定）                                                 | 事务、触发器、`CHECK` 约束、对账              |
| 会员权益       | 等级折扣 + 积分抵现（无储值、**不降级**）                                                    | 价格快照、派生数据与回滚                      |
| **线上预订建模** | **「已预定」只是计数**（`reserved_qty`），**不占状态位**；**预订不冻结金额 / 折扣 / 积分**，一切以自提时为准（R11） | 状态函数化（**一元**）、`CHECK` 约束表达业务规则    |
| 应用层        | B/S + 手写 SQL                                                                | ——                                |

### 规模口径

约 **27 张表** · **11 个核心实体** · **7 个规则域**（`01` §2.1 ~ §2.7） · **10 个前端页面** ·  
**商品状态 4 态**（自动区 2 + 人工区 2） · **5 个数据库技术点**  
（事务 / 触发器 / 存储过程 / 视图 / 索引与约束）。

---

## 3.1 已拍板的业务规则

| 编号  | 规则                    | 定案                                                                                                                                |
| --- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| R1  | 折扣与积分是否叠加、以何顺序        | 可叠加，**先扣积分再打折**                                                                                                                   |
| R2  | 是否允许部分退货              | 允许，**按订单行退货**                                                                                                                     |
| R3  | 会员是否降级                | **不降级**，等级只升不降                                                                                                                    |
| R4  | 积分随折扣缩水是否可接受          | **接受**，这是 R1 的必然结果，非缺陷                                                                                                            |
| R5  | 「能不能卖」怎么判定            | **判商品种类，不判单册**。可销售条件 = `(在库量 − 预定量 − 损坏量) > 0`，**恒等于 `stock_qty > 0`**。`reserved_qty` 只是计数，**不参与状态判定**                            |
| R6  | 价格 / 折扣 / 积分的**快照时点** | **只在成交时快照**（门店结算、线上自提核销），此后不回改。**线上预订不产生快照** —— 见 R11                                                                             |
| R7  | 商品种类与具体商品的区分粒度        | **只到「计数」级**：种类 = `Product` 一行；单册**无独立记录**，体现为 `stock_qty` / `reserved_qty` 两个计数。**不做副本表**                                         |
| R8  | 报损是否落独立的「待损池」         | **不落**。`DAMAGE_OUT` 即时扣减 `stock_qty`；将来若要「破损待处理区」，新增占用池即可，**状态机无需改动**                                                             |
| R9  | 商品状态机的层次              | **4 态，分两层**：自动区 `ON_SALE` / `OUT_OF_STOCK` 由 `stock_qty` **一元判定**；人工区 `OFF_SHELF` / `DISCONTINUED` 由人决定，触发器不碰                     |
| R10 | 「全部在库商品均被预定」怎么表达      | **归入 `OUT_OF_STOCK`**，不单列状态。由 `stock_qty = 0` 且 `reserved_qty > 0` 表达，并在查询视图展示计数；**是否补货由库管自行判断，系统不提示**                            |
| R11 | 线上预订是否冻结金额 / 折扣 / 积分  | **一律不冻结**。预订单只锁 `qty` 个商品；金额、折扣、积分**以线下自提当时为准**。`Reservation` / `ReservationItem` **不含任何金额字段**                                    |
| R12 | 盈亏口径                  | **拆成两个指标**：经营盈亏 = `SALE_IN − PURCHASE_OUT − REFUND_OUT − OPERATING_EXPENSE`；现金余额 = `SUM(CashFlow.amount_delta)`。**注资 / 提现不进经营盈亏** |
| R13 | 建档时商品的初始状态            | 建档即按 `fn_ProductStatus` 判定 —— `stock_qty = 0`，故为 **`OUT_OF_STOCK`**；进第一批货后自动转 `ON_SALE`                                           |
| R14 | 派生字段的触发器职责划分          | **库存流水触发器**维护 `stock_qty`（对所有 `movement_type`）并重跑 `fn_ProductStatus` 维护 `status`；**预订单触发器**只维护 `reserved_qty`                     |

> **待商议项**（本阶段 block，不拍案）：采购审批环节、`ref_type` 取值语义、  
> 退货积分返还方案等 —— 完整清单见 `01-scenario.md` §6.1。

---

## 4. 技术栈

| 层     | 选型                      | 说明             |
| ----- | ----------------------- | -------------- |
| 数据库引擎 | SQL Server Express 2022 | 主战场            |
| 数据库工具 | SSMS                    | 建库、调试、执行计划     |
| 应用层   | Python 3 + Flask        | 刻意保持薄，无 ORM 抽象 |
| 数据库驱动 | pyodbc + ODBC Driver 18 | 直接执行 T-SQL     |
| 模板引擎  | Jinja2                  | 服务端渲染，页面简单     |
| 前端样式  | 单文件 CSS，无构建工具           | 简洁优先           |

**选型理由**：小组对 C/C++ 最熟，Web 层不是学习目标。  
Flask 代码量最小，每一条 SQL 都直接可见，最贴合「学 SQL」的诉求。  
若小组最终更倾向 JS/TS，可平替为 Express + `mssql`，下方开发铁律不变。

---

## 5. 非目标（写进文档，防止范围蔓延）

- 二手书单册管理
- 储值 / 预付卡
- 网店物流、第三方支付对接
- 推荐算法、实时消息推送
- 微服务、分布式、多仓调拨
- 行级数据权限、动态权限
- **会计口径的毛利核算**（移动加权平均成本）——  
  经营盈亏只做收付实现制（另设现金余额指标），见 `01-scenario.md` §2.7

提前声明边界，既省掉无用讨论，也向老师展示这是一个**有边界的设计**。

---

## 6. 开发铁律

1. **建表只能用 DDL 脚本 + SSMS 执行**，禁止任何 ORM 的自动迁移。
2. **业务规则优先下沉到数据库**：能用约束就不写代码，能用触发器就不写在应用层，  
   能用存储过程就不在应用层拼 SQL。
3. **每张表、每个字段都必须有注释**（`sp_addextendedproperty`）。
4. **不允许 `SELECT *`**，查询必须显式列名。
5. **每条业务流必须能在一个事务内完成**，并且能在测试中被回滚验证。
6. **禁止拼接 SQL 字符串**，一律参数化。
7. **派生字段只读不写**：`Product.stock_qty`、`Product.reserved_qty`、`Product.status`、  
   `Member.points_balance` 一律由触发器维护，应用层不得直接 `UPDATE`。
8. **单册不建档**：商品按**种类**建模（一个种类一行），单册只用计数表达，  
   不建副本表。判断「某册」的需求一律退化为判断「种类 + 数量」。
9. **状态只看一个量**：商品状态（自动区）只由 `stock_qty` 决定，  
   `reserved_qty` 只用于展示与对账，**不得参与状态推导**。  
   新增任何占用口径都不改状态机 —— 因为占用在可销售条件里会自我抵消（R9）。

---

## 7. 文档索引

| 文档                      | 内容                        | 状态         |
| ----------------------- | ------------------------- | ---------- |
| `00-overview.md`        | 项目总览与开发约定                 | 本文         |
| `01-scenario.md`        | 情景设定书：书店档案、经营规则、运营轨迹、资金规则 | **v7.1 待审阅** |
| `04-data-dictionary.md` | 数据字典：表名 / 字段 / 类型 / 约束    | 未开始        |
| `db/05-ddl.sql`         | 建库建表脚本（含注释与约束）            | 未开始        |
| `06-prd-m1.md`          | 第一阶段 PRD 与验收标准            | 未开始        |
| `07-test-plan.md`       | 测试纲要（**语义层**）：业务场景 / 角色权限 / 数据自洽性 | **v8 待审阅** |

> ⚠️ **已移除的文档（v8）**：原先的 `02-data-flow.md`（数据流动地图）与
> `03-er-model.md`（ER 模型）**已删除** —— 第一阶段只做初步拍案，
> 表级数据流动与实体关系属于细节层面，**相关内容一律留待后续讨论**。

> **v8 改动摘要**（2026-09-11）：按「第一阶段只做初步拍案、不敲细节」的方针**收缩文档范围** ——
>
> 1. **删除** `02-data-flow.md` 与 `03-er-model.md`，相关内容留待讨论。
> 2. **`07-test-plan.md` 整体瘦身**为**语义层测试纲要**：移除数据基线数值、逐用例步骤表、
>    预期数值表、边界用例编号与**全部验证 SQL**，只保留「**测什么、达到什么语义标准**」。
> 3. 不变式清单（原在 `02` 内）随之一并移除，其**语义**已并入 `07` §5。
> 4. `01-scenario.md` 保持不变 —— 它是本阶段的事实来源。
>
> **当前文档清单**：`00`（总览）· `01`（情景与规则）· `07`（测试纲要），
> 以及尚未开工的 `04`（数据字典）/ `05`（建库脚本）/ `06`（M1 PRD）。

> **v7 改动摘要**（2026-09-11，一致性审查定案）：本次只修**语义与描述**，不动已冻结的决策。
>
> 1. **建档初始状态改判**：建档即按 `fn_ProductStatus` 判定 → **`OUT_OF_STOCK`**（R13）。
>    原稿「建档即 `ON_SALE`」与一元判定矛盾，已修正（`01` §2.1.1 / §3 / §7、`07` TC-B1 / TC-B2）。
> 2. **触发器职责划分**（R14）：库存流水触发器统一维护 `stock_qty` 与 `status`；
>    预订单触发器只维护 `reserved_qty`。消除线上预订流程的**双重扣减**隐患。
> 3. **线上预订不冻结任何东西**（R11）：金额、折扣、积分一律以自提时为准；
>    `Reservation` / `ReservationItem` 的全部金额字段删除。
> 4. **盈亏口径拆分**（R12）：经营盈亏（不含注资 / 提现）与现金余额分列。
> 5. **采购审批环节 block**（Q1）：删除审批人字段与相关权限描述，状态机去掉 `CLOSED`。
> 6. **撤销 I9 / D9**（与 I15 / D15 语义重复；两表已随 v8 移除）。
> 7. **删除孤字段与未使用字段**：`Product.safety_stock`、各处 `points_used`。
> 8. **新增 §6.1 待商议项清单**（Q1 ~ Q8）—— 本阶段不做细节敲定，相关细节一律留空。

> **v6 改动摘要**（2026-09-11）：**「已预定」从商品状态层下线，退回为纯计数** ——
>
> 1. 商品状态机由 5 态收敛为 **4 态**：自动区 `ON_SALE` / `OUT_OF_STOCK`  
>    （由 `stock_qty` **一元判定**），人工区 `OFF_SHELF` / `DISCONTINUED`；  
>    `SOLD_OUT` 更名 `OUT_OF_STOCK`。
> 2. 判定函数退回一元 `fn_ProductStatus(@stock_qty)`。
> 3. 可销售条件统一表述为 `(在库量 − 预定量 − 损坏量) > 0`，  
>    并证明它**恒等于 `stock_qty > 0`** —— 占用池的加减互相抵消，  
>    因此**新增占用口径不需要改状态机**。
> 4. `Product.reserved_qty` **保留字段**，但降为纯计数，只用于展示与对账（R5）。
> 5. 「全部在库商品均被预定」**归入缺货**，不单列状态；是否补货由库管判断，  
>    系统**只呈现计数、不给建议**（R10）。
> 6. **新增 R8 / R9 / R10**；**新增铁律 9**（状态只看一个量）。
> 7. **盘点基准 `physical_qty` 与不变式 I11 / I14 不受影响**（这两条是 v4 的正确部分）。
>
> **v4 改动摘要**（2026-09-11）：**「已预定」改以商品状态身份进入体系** ——  
> `Product.status` 扩为 5 态，新增 `RESERVED`；  
> `Product` 新增 `reserved_qty` 字段，与 `stock_qty` 构成**两个计数池**，  
> 商品状态由两池唯一决定（`fn_ProductStatus`），不再另立状态机。  
> 由此**盘点基准反转为 `physical_qty`**（已预定的货仍在架上），  
> v3 的「标为已预定并跳过」补丁作废。  
> 商品粒度明确为**计数级**：一个种类一行，单册不建档（R7）。  
> （**v6 已撤销其中「`RESERVED` 是一个状态」这一条**，其余结论保留。）
>
> **v3 改动摘要**（2026-09-10）：把「预订」从销售订单里解耦成独立实体  
> （`Reservation` / `ReservationItem`，25 → 27 张表）；  
> `SalesOrder` 状态机简化为 `PAID` / `PARTIAL_REFUND` / `REFUNDED`。

---

## 8. 阶段划分

- **M1（第 1 月）· 需求与建模**  
  产出情景设定书（`01`）与测试纲要（`07`），并推进数据字典（`04`）与建库脚本（`05`）；  
  最小闭环可运行：创店建档 → 采购收货 → 一次销售 → 库存与资金变化可查。
- **M2（第 2 月）· 业务补全**  
  会员权益、**线上预订与自提核销**、退货、盘点、资金收付、RBAC、审计；  
  补齐触发器与存储过程（含 `usp_ExpireReservations`）。
- **M3（第 3 月）· 报表与交付**  
  盈亏与库存报表、UI 完善、测试数据、按 `07-test-plan.md` 全量回归、答辩材料。

---

## 9. 三人分工

| 角色        | 负责范围                           | 阶段交付物            |
| --------- | ------------------------------ | ---------------- |
| A · 数据架构  | ER、关系模式、3NF、DDL、约束、索引、触发器与存储过程 | 数据字典 + 建库脚本      |
| B · 交易链路  | 采购、销售、退货、库存与资金流水的后端逻辑与事务       | 业务存储过程 + 应用层接口   |
| C · 会员与前台 | 会员权益、RBAC、审计、盈亏报表、前端页面         | 页面 + 权限校验 + 报表视图 |

**协作铁律**

1. A 必须在 M1 内**冻结数据字典**（表名、字段名、类型、约束、外键）。  
   B 与 C 之后基于这份契约并行开发，不再改表结构。
2. 三人开工前先对齐命名规范 —— 三人小组最常见的翻车点就是第二阶段大面积返工。
3. 任何业务模块在提交前，必须能在 `07-test-plan.md` 里找到对应的**测试语义**并满足它。
